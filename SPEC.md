# HongGoCat — 사양 · 요청 이력 · 결정 사항

> **작업 규칙**: 수정 요청이 오면 **먼저 이 문서를 읽고**, 여기 확정된 동작(§2)을 깨지 않도록 작업한다.
> 새 요청/수정은 아무리 사소해도 §2·§3에 추가·갱신한다. (회귀 방지)

최신 갱신: 2026-07-13

---

## 1. 개요
- **HongGoCat**: Steam Bongo Cat 스타일의 **전체화면 데스크톱 오버레이**. 키보드/마우스 입력에 맞춰 고양이가 연주. 방 코드로 친구와 멀티, 커서 유도 미사일로 서로 맞히기.
- 스택: Electron(메인) + Canvas 렌더 + `uiohook-napi`(전역 입력) + `ws`(릴레이 서버).
- 실행: `npm install` → 서버 `npm run server` → 오버레이 `npm start`.

## 2. 확정 동작 (⚠️ 회귀 금지 — 요청 없이 되돌리지 말 것)

### 창 / 오버레이
- **전체화면 투명 오버레이**. 한 번에 **한 모니터**를 덮음(Windows가 투명창을 단일 모니터로 클램프하므로 다중 모니터 동시 span 불가).
- **클릭 통과(click-through)**가 기본. 커서가 **고양이 위젯(hotzone) 위에 있을 때만** 클릭 가능(메인 프로세스가 실제 커서를 24ms로 폴링해 `setIgnoreMouseEvents` 토글).
- **상단 흰 바 — 진짜 원인·해결 확정(픽셀 측정으로 규명)**:
  - 원인: **투명 창의 GPU 합성**이 창이 활성(foreground)일 때 y=0에 ~22px 흰 바를 그림(Electron+Windows 투명창 버그). 픽셀 측정으로 활성 시 (250,253,255) 바 확인.
  - **해결(핵심)**: `main.js` 맨 위 **`app.disableHardwareAcceleration()`** → 활성/클릭/설정열기 등 어떤 상태에서도 바가 안 뜸(강제 활성화 테스트로 확인). ⚠️ **이 줄을 지우면 흰 바 재발** — 절대 제거 금지.
  - 시도했으나 효과 없던 것들(다시 하지 말 것): `focusable:false`(오히려 항상 바), WS_EX_NOACTIVATE(직접), 1px 축소(최대화 회피), DWM BORDER_COLOR/NCRENDERING, blur-on-focus/poll-blur.
  - 보조로 남긴 것: DWM `BORDER_COLOR=NONE`+`CORNER_PREFERENCE=DONOTROUND`(테두리/둥근모서리), 시작 시 `showInactive`, 커서 hotzone로 클릭 통과 토글.
  - 참고: HW 가속 off → 캔버스 소프트웨어 렌더. 가벼운 오버레이라 OK지만, 렉 발생 시 최적화(유휴 시 FPS 다운 등) 여지.
- **단일 인스턴스**만 실행(중복 실행 시 기존 창 포커스). 유령 창 방지.
- 창 타이틀 비움.

### 캐릭터 (고양이 전용)
- **고양이만** 지원(개/돼지/원숭이 제거됨 — 되살리지 말 것).
- 스타일: 흰 기본, 분홍 볼터치, 감은 눈웃음 + 자연스러운 눈 깜빡임(랜덤 윙크 아님), 외곽선, 꼬리(살랑), 광택 하이라이트, 그라데이션 입체감.
- **팔(몸통→발 연결선) 없음**. 앞발은 작은 패드만. 키 입력 시 좌/우 앞발이 키보드를, 마우스 클릭 시 오른발이 마우스로 이동해 누름.
- 위젯 = [고양이 + 책상(키보드/마우스) + 하단 바]. 하단 바 폭 = 고양이 폭(정렬 일치). 위젯은 **하단 중앙**에서 시작, **드래그로 그 모니터 안 어디든** 이동.

### 커스터마이즈 (설정 창)
- **털색 9종**: 흰/크림/회색/갈색/검정/치즈/분홍/민트/라벤더.
- **무늬 5종**: 단색/태비/턱시도/점박이/포인트.
- **모자 6종**: 없음/비니/파티/왕관/중산모/야구모자.
- **디자인 편집 모드**: 귀·눈·꼬리 위치를 핸들 드래그로 조정, `localStorage.feat`에 저장.
- 이름 설정. 모든 값 localStorage 저장·유지, 멀티 접속 중이면 실시간 반영.

### 하단 바 / 카운터 / 메뉴
- 하단에 **둥근 UI 바**: 왼쪽 **카운터**(햄버거 옆까지 넓게), 오른쪽 **햄버거(≡)**.
- **카운터** = 내 상호작용 누적 횟수. **애니메이션 없음**(통통 튀지 않음). localStorage 저장·누적.
- 카운트 규칙: **키는 처음 누를 때 1회만**(꾹 눌러도 반복 안 됨), **클릭 1회**, **스크롤(휠)은 카운트 안 함**.
- **햄버거 → 설정 창 토글**(열려 있으면 닫힘). 별도 floating 창, 한 번 클릭에 앞으로 뜸. 오버레이 창 크기 불변.

### 채팅
- `Ctrl+Shift+B`(또는 설정의 채팅 버튼) → 입력창. Enter 전송/Esc 취소. 머리 위 말풍선(최대 80자). 채팅 때만 잠깐 창을 포커스 가능하게 함.

### 무기 슬롯 / 미사일
- **무기 슬롯 3개**: `Ctrl+1 / Ctrl+2 / Ctrl+3`. 각 슬롯에 무기를 설정 창에서 지정(현재 무기: 없음 / 🚀미사일, 추후 추가 예정). 단축키 누르면 그 슬롯 무기 발사. uiohook로 **초기 press 1회만**(꾹 눌러도 1발). (⚠️ Ctrl+1/2/3은 브라우저 탭 전환과 겹칠 수 있음 — 필요 시 조합 변경 가능)
- 무기 dispatch는 `fireWeapon(id)`에서 분기(확장 지점). `me.slots`는 localStorage 저장.
- **무기 종류**: `none` / `missile`(🚀) / `shield`(🛡). `WEAPONS` 등록 + `fireWeapon(id)` 분기로 확장.
- **🛡 쉴드**: 캐릭터 중앙 기준 **140° 아크 방어막**이 **커서 방향**을 향함. **바깥쪽 테두리(rim)만** 그림 — 속을 채운 부채꼴 아님. 반경 `SHIELD_R=108×scale`로 캐릭터 바깥에 넓게 펼쳐짐. **10초 지속 → 3초 쿨타임**(쿨/지속 중 재발동 불가). 지속 중 미사일이 아크 범위에 닿으면 막혀 폭발(캐릭터 명중 방지).
  - **체력(HP=10)**: 미사일 10회 막으면 깨짐. HP가 줄수록 rim이 **흩어진 순서로 조각조각 갈라지고**(온전 세그먼트 수 감소) 색이 **청록→빨강**으로 물들며, HP<35%면 **깜빡임(hurt flicker)**. **숫자 UI 없음** — 균열 정도로 체력 표현. HP 0 → **유리 파편이 아크 방향으로 튀는 파괴 연출**(`shieldShards`) 후 소멸, 쿨타임은 파괴 시점부터.
  - 종료 시엔 마지막 1초간 **점점 투명 + 깜빡임**으로 자연스럽게 사라짐(파괴가 아닌 시간만료).
  - **막기 규칙**: 이 클라이언트는 **자기 미사일만** 시뮬레이트하므로 쉴드는 **자기 미사일을 먹지 않음**(`shieldBlocks`가 `me` 스킵). 내 미사일이 상대 쉴드에 막히면 `shield-hit`을 그 상대에게 전송 → 상대가 자기 HP 차감. 반대로 상대 미사일이 내 쉴드에 막히면 상대가 `shield-hit`을 나에게 보냄.
  - 멀티: 쉴드 상태(각도+남은시간+**hp/max**)를 90ms로 브로드캐스트 → 상대 화면에도 균열까지 렌더. 파괴 시 `{ttl:0, broke:true}`로 파편 연출 동기화. 서버 릴레이: `shield`(hp/max/broke 포함), `shield-hit`.
- **미사일 활성 개수 = 파워 합산**(합쳐진 power-5 미사일 = 5로 카운트). 상한 `MAX_MISSILES=6`(총 파워). 터지거나 사라져 파워가 줄면 재발사 가능.
- **미사일**(기본 슬롯1=미사일, `Ctrl+1`). 아래 동작 유지:
- **자기 고양이 머리 위**에서 발사 → **마우스 커서를 유도 추적** → 고양이에 충돌 시 폭발 + **X눈·별 리액션**.
- **동시 최대 6발**, 유지시간 **14초**. 터지거나 사라지면 다시 발사 가능.
- **병합**: 미사일끼리 가까워지면 합쳐져 `power`↑ → **크기 커지고 빨강→황금색 + 반짝임 + 더 큰/황금 폭발**.
- **멀티**: 내 미사일 위치를 **내 고양이 기준 상대좌표**로 90ms마다 브로드캐스트 → 상대에게도 보임. 내 미사일 ↔ 상대 미사일 충돌 시 폭발. 내 미사일이 상대 고양이 명중 시 `hit` 전송 → 상대 화면에서도 반응.

### 멀티플레이 (서버 릴레이)
- `server/server.js`: 방 코드 입장(방당 최대 12명, `MAX_ROOM_SIZE`). 릴레이 메시지: `join/roster/pulse/chat/throw/missiles/hit/shield/update`.
- `joined` 메시지에 `max`(방 정원) 포함. 오버레이는 `roster.peers.length`로 **현재 인원**을 집계해 설정 창에 push → 설정 창 멀티플레이 섹션에 **"접속 인원: N / 12명"** 표기(오프라인이면 "— / 12명 (오프라인)"). 인원은 roster 갱신·접속·해제 시 실시간 반영.
- 프라이버시: 네트워크로는 입력 **종류(key/mouse)** 와 미사일/채팅만 전송. 어떤 키인지는 전송 안 함(초당 25펄스 제한).

### 모니터
- 설정 창 **🖥 다음 모니터로 이동** 버튼으로 오버레이를 다른 모니터로 이동(전역 단축키는 없음 — `Control+Shift+Period`가 Electron에서 무효라 제거함).

## 3. 요청 이력 (시간순 요약)
1. 멀티 봉고캣 오버레이 제작(초기엔 여러 동물) → 이후 **고양이 전용**으로 축소.
2. 채팅(말풍선) + 오버레이에서 채팅 입력 방식(단축키).
3. 레퍼런스풍 귀여운 흰 고양이 + 카운터 + **상단 바 숨기기**.
4. 멀티 방법 안내 + 커스터마이즈(모자/색).
5. 캐릭터 퀄리티 업, 키보드 크기, 표정 애니메이션 → **고양이만** 진행.
6. **기괴한 팔 제거**, 마우스 클릭 애니메이션.
7. 카운터를 하단 바 UI로, 고양이 하단 바에 맞춰 크롭, **상단 창 바 투명/제거**.
8. 미사일(발사·궤적·폭발·명중 반응), 디자인 편집 모드, 무늬, **설정 별도 창**.
9. 카운터 애니메이션 제거, 스크롤 카운트 제외, **꾹 눌러도 1회**.
10. 미사일 **단축키 + 커서 추적 + 충돌**, 햄버거에서 제거.
11. **HongGoCat**로 개명, **전체화면**에서 미사일/채팅.
12. 미사일 유지시간↑, **최대 개수 제한 + 재발사**, 업데이트 방법, **햄버거 2클릭 버그**, **멀티 미사일 상호 표시 + 미사일 충돌**, 듀얼모니터.
13. 미사일 **자기 캐릭터에서 발사**, **상단 바(설정창 닫을 때 등) 완전 제거**, **본 SPEC.md 작성**.
14. 🛡 쉴드 무기(부채꼴/커서 방향/3초·쿨3초/깜빡이며 소멸), 미사일 활성수 = 파워 합산.
15. **배포/셋업 안내**(폴더 공유 or exe 패키징), **자동 업데이트 가능성 안내**(electron-updater+GitHub Releases), 설정 창에 **접속 인원 / 정원 표기** 추가.
16. 쉴드 **10초 지속**, **체력(HP 10, 미사일 10발)** 도입 — HP 감소 시 rim이 갈라지고 색 변화/깜빡임, HP 0에 **파편 파괴 연출**. 쉴드 모양을 **바깥으로 넓힌 테두리형(140° rim, 채움 없음)**으로 변경.

## 4. 알려진 한계 / TODO
- **다중 모니터 동시 커버(⑨)**: 한 창으로 불가(Windows 클램프). 필요 시 **모니터마다 오버레이 창 1개**씩 띄우는 멀티창 구조 필요(미구현).
- **멀티 미사일**: 서버 릴레이·수신은 2-클라이언트 프로토콜 테스트 통과. **실제 2인 동시 접속 시각 검증은 미완**(위치 어긋남 등 실사용 확인 필요).
- **상단 바**: 합성 입력으로는 재현이 어려워, 재적용(reassert) 방어로 대응 중. 재발 시 "정확히 언제" 정보를 받아 추적 필요.
- **업데이트 배포**: 현재 git 저장소 아님. 원하면 git init + `.gitignore`(node_modules 제외)로 `git pull` 업데이트 체계 구성 가능.

## 5. 단축키 요약
- `Ctrl+Shift+B` 채팅 · `Ctrl+1/2/3` 무기 슬롯 발사 · 편집 모드/모니터 이동/무기 지정은 설정 창에서.
